# TODO — تکمیل پروژه سیستم مدیریت یادداشت شخصی

بر اساس بررسی `project_discription.md` در برابر کد فعلی (`Note.py`, `NoteManager.py`, `cli_io.py`, `__main__.py`) و تست‌های عملی انجام‌شده.

## 🐛 باگ‌های تأیید شده (اولویت بالا)

- [x] **ID برخورد می‌کند و داده گم می‌شود** — در `NoteManager.handle_create_note`، ID جدید با `len(self.notes) + 1` ساخته می‌شود. اگر یادداشتی حذف شده باشد، ID بعدی می‌تواند با یک ID موجود برخورد کند و آن یادداشت را در حافظه و فایل بازنویسی/گم کند (تأیید شده با تست: حذف id=1، ساخت جدید → دوباره id=2 به آن داده شد و یادداشت قبلی گم شد).
  - راه‌حل پیشنهادی سند: استفاده از `uuid.uuid4()` برای تولید ID یکتا به‌جای شمارش طول دیکشنری.
- [x] **`read_multiline_input` کل ورودی باقی‌مانده را می‌بلعد** — چون فقط با `EOFError` متوقف می‌شود (نه با خط خالی یا کلمه پایان)، بعد از `note create` هیچ دستور دیگری در همان session قابل اجرا نیست؛ کل stdin تا انتها به‌عنوان content خوانده می‌شود.
  - راه‌حل پیشنهادی: پایان دادن با یک خط خالی پشت‌سرهم، یا یک سطر مشخص مانند `EOF`/`END`، یا (در حالت Typer/CLI تعاملی واقعی) استفاده از روش استاندارد چندخطی.
- [x] **کاراکتر کنترلی داخل محتوا ذخیره می‌شود** — در `notes.json` واقعی، کاراکتر `` (EOF ویندوز) داخل content یادداشت id=3 ذخیره شده؛ نتیجهٔ مستقیم باگ بالا.
- [x] **کرش روی EOF به‌جای خروج تمیز** — حلقهٔ اصلی در `NoteManager.init` هیچ `try/except EOFError`ی ندارد؛ با بسته شدن ورودی، برنامه با traceback کرش می‌کند به‌جای پیام خروج مناسب.
- [x] **دو پیام خطا پشت‌هم برای ID نامعتبر** — وقتی ID عدد نیست، هم `extract_note_id` هم `find_note_by_id`/`show_note_by_id` پیام خطا چاپ می‌کنند (نه کرش، فقط UX نامرتب).
- [x] **فرمت پیام تأیید حذف مطابق سند نیست** — سند نمونهٔ پیام می‌خواهد: «آیا از حذف یادداشت با ID X مطمئن هستید؟ [y/N]»؛ `last_check` فعلاً پیام عمومی «are u sure?» دارد و ID را نشان نمی‌دهد. (پیام انگلیسی با ID شد تا مشکل UnicodeEncodeError روی کنسول ویندوز پیش نیاد؛ اگه بعداً خواستید دقیقاً فارسی مطابق سند + `[y/N]` باشه، باید encoding خروجی رو با `sys.stdout.reconfigure` حل کنید.)

## 🏗️ ساختار پروژه (cookiecutter flat layout)

- [x] فایل‌ها را داخل یک پکیج به نام `note/` ببرید (نه فایل‌های flat در ریشه با نام‌های فعلی).
  - [x] `Config.py` فعلاً عمداً توی ریشهٔ پروژه نگه داشته شده (import مطلق `from Config import Config` توی `services.py`)؛ بعداً باید به `note/config.py` منتقل بشه و importش نسبی (`from .config import Config`) بشه.
- [x] `note/__main__.py` بسازید — نقطهٔ ورود برای `python -m note`، شامل دستورات Typer.
- [x] ماژول‌ها را با مسئولیت مشخص جدا کنید: مثلاً `models.py` (کلاس `Note`)، `storage.py` (خواندن/نوشتن JSON — جایگزین `cli_io.py` فعلی)، `services.py`/`crud.py` (منطق `NoteManager`)، `web_app.py` (اپ FastAPI)، `routers/` (route های وب)، `templates/` (فایل‌های Jinja2).
  - [x] `cli_io.py` فعلی دو مسئولیت قاطی داره: ذخیره‌سازی فایل (`load_config`/`save_config`/`load_notes`/`write_note`/`save_all_notes` → باید برن `storage.py`) و تعامل با کاربر توی ترمینال (`read_input`/`read_multiline_input`/`init_message`/`note_list_show`/`note_show`/`show_message`/`error`/`exit_note_manager`). باید تصمیم بگیریم توابع دستهٔ دوم کجا برن (فایل جدا مثل `console.py`، یا بعداً با دستورات Typer جایگزین بشن).
- [x] `pyproject.toml` بسازید شامل:
  - [x] `[tool.coverage.run]` با `omit` برای `note/__main__.py` و `note/web_app.py`
  - [x] بخش `tool.ruff.lint` — **تغییر ندهید**، فقط مطمئن شوید کد بدون خطای lint اجرا می‌شود.
  - [x] dependency ها (`typer`, `fastapi`, `uvicorn`, `jinja2`, `pytest`, `pytest-cov`).

# ======= تا اینجا باید کار بشه
## ⌨️ CLI با Typer

- [x] جایگزینی کامل حلقهٔ دستی فعلی (`pars_input` + رشته‌خوانی) با دستورات Typer:
  - [x] `note create` — تعاملی: اول title سپس content (چندخطی)، نمایش ID جدید بعد از ساخت.
  - [x] `note list` — نمایش خلاصه (ID + title) همهٔ یادداشت‌ها.
  - [x] `note show <id>` — نمایش همهٔ جزئیات؛ پیام خطای مناسب برای ID نامعتبر.
  - [x] `note update <id>` — نمایش جزئیات فعلی، پرسش این‌که کدام فیلد ویرایش شود، دریافت مقدار جدید، به‌روزرسانی `last_modified_date`.
  - [x] `note delete <id>` — پیام تأیید قبل از حذف (`[y/N]`).
  - [x] `note web [--port PORT]` — اجرای Web UI (uvicorn) با پورت اختیاری.
  - [x] بررسی خروجی `python -m note --help` مطابق نمونهٔ سند.

## 🌐 Web UI با FastAPI + Jinja2

- [x] ساخت اپ FastAPI در `web_app.py`.
- [x] Route ها:
  - [x] `GET /` → redirect خودکار به `/notes`
  - [x] `GET /notes` → صفحهٔ لیست یادداشت‌ها
  - [x] `POST /notes` → ایجاد یادداشت جدید از فرم وب
  - [x] `GET /notes/{note_id}` → صفحهٔ نمایش جزئیات یک یادداشت
  - [x] صفحه/route فرم ویرایش (`GET`/`POST` برای update)
  - [x] route حذف (`POST` یا `DELETE`)
- [x] Template ها با Jinja2:
  - [x] یک template پایه (base layout) با `{% block %}` برای وراثت بین صفحات
  - [x] صفحهٔ لیست (حلقهٔ Jinja2 روی یادداشت‌ها)
  - [x] فرم ایجاد یادداشت جدید
  - [x] صفحهٔ جزئیات یک یادداشت
  - [x] فرم ویرایش یادداشت
- [x] UI ساده کافی است (تمرکز روی کارکرد Jinja2/FastAPI، نه طراحی پیچیده).

## 🧱 شیءگرایی (OOP)

- [x] کلاس `Note` را کامل‌تر کنید: کپسوله‌سازی مناسب attribute ها، و متدهای کاربردی (مثلاً `to_dict`/`from_dict` برای تبدیل به/از JSON، به‌جای استفاده مستقیم از `note.__dict__` در `cli_io.py`).
- [x] جدا کردن مسئولیت‌های `NoteManager` از I/O مستقیم (چاپ پیام، گرفتن input) — طبق سند، منطق اصلی (business logic) باید بدون وابستگی به CLI قابل تست باشد. در حال حاضر متدهای `handle_*` و حتی برخی متدهای اصلی مستقیماً از `cli_io.read_input`/`print` استفاده می‌کنند که تفکیک UI از منطق را نقض می‌کند.
- [x] تغییر ID از `int` به `str` (uuid) در کلاس `Note` و همه‌جا که از نوع `int` برای id استفاده شده.

## 🎯 Decorator ها

- [x] استفادهٔ طبیعی از decorator های Typer (`@app.command`) و FastAPI (`@app.get`, `@app.post`) در پیاده‌سازی بالا (این‌ها به‌خودی‌خود از این بخش پوشش می‌دهند).
- [x] حداقل یک decorator سفارشی بنویسید، مثلاً:
  - [x] decorator برای لاگ کردن زمان اجرای یک تابع خاص
  - [x] decorator برای مدیریت یکپارچهٔ یک نوع خطای خاص در چند تابع (مثلاً خطاهای فایل یا ID نامعتبر) — فعلاً `try/except` تکراری توی هر سه‌تا `handle_show_note`/`handle_update_note`/`handle_delete_note` نوشته شده (کار می‌کنه)؛ بعداً با این decorator یکپارچه بشه

## 📝 Logging

- [x] راه‌اندازی ماژول `logging` با خروجی به فایل `note_manager.log`.
- [x] سطح‌بندی مناسب:
  - [x] `INFO` برای عملیات موفق (create/update/delete/load/save)
  - [x] `ERROR` برای خطاها (فایل پیدا نشد، JSON خراب، ID نامعتبر و غیره)
  - [x] `DEBUG` برای جزئیات دیباگ

## 💾 ذخیره‌سازی JSON

- [x] تغییر فرمت فایل از JSON Lines فعلی به یک آرایهٔ JSON واقعی (`[ {...}, {...} ]`) مطابق سند.
- [x] مدیریت خطاهای فایل: عدم وجود فایل، JSON خراب (corrupt)، مشکلات دسترسی — با پیام/لاگ مناسب به‌جای crash یا سکوت کامل.
- [x] اطمینان از این‌که ذخیره بعد از هر تغییر مهم (و/یا هنگام خروج) انجام می‌شود.

## ✅ pytest

- [x] نوشتن تست‌های واحد برای منطق اصلی، **مستقل از CLI/Web UI**:
  - [x] افزودن یادداشت (و تخصیص صحیح ID یکتا، حتی بعد از حذف‌ها)
  - [x] جستجوی یادداشت بر اساس ID (پیدا شدن / پیدا نشدن)
  - [x] به‌روزرسانی یادداشت (تغییر `last_modified_date`، حفظ `creation_date`)
  - [x] حذف یادداشت
  - [x] ذخیره/بارگذاری از JSON (شامل حالت فایل خراب/ناموجود)
- [x] اجرا با `python -m pytest --cov` بدون خطا و coverage بالای ۸۰٪.

## 🔍 ابزار کیفیت کد

- [x] اجرای `ruff` بدون هیچ خطایی (بدون تغییر `tool.ruff.lint`).
- [x] بررسی نهایی `python -m note --help` که خروجی مطابق نمونهٔ سند بدهد.
